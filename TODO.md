## Next Steps

Application -> owns the lifecycle and main loop
│
├── Backend -> platform specific stuff
│
├── InputManager -> converts raw  backend inputs into your common events
│
├── EventManager -> Routes event
│
├── AnimationManager -> manages currently running animation
│
├── WidgetTree 
│
├── LayoutEngine
│
└── Renderer
